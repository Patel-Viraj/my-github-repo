pipeline {
         agent any
         stages {
                 stage('Copy to S3') {
                  steps {
                      pwd
                      aws --version
                  }
                 }
                 stage('build') {
                  steps {
                        echo "App is building"
                   }
                 }
             
      }
}
