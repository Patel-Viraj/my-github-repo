pipeline {
         agent any
         stages {
                 stage('Copy to S3') {
                  steps {
                     echo 'staring build the app'
                  }
                 }
                 stage('build') {
                  steps {
                        echo "App is building"
                   }
                 }
             
      }
}
